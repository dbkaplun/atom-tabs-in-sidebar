'use babel';

import AtomTabsInSidebarView from '../lib/atom-tabs-in-sidebar-view';

describe('AtomTabsInSidebarView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
